qunction.js (pronounciation: queue nction)

a library which implements enqueing functions to be executed in a non blocking manner

foo = Qunction([arrayOfFunctions]);
foo.add(someFunction[, position]);
foo.adds(arrayOfFunctions/QunctionInstance[, position])
foo.remove(someOtherFunction/position);
foo.start()
foo.pause()
foo.stop()
